const { Router } = require('express');
const router = Router();

// TODO: import controllers and register their routes here.
// Example:
// const userController = require('../controllers/user.controller');
// router.use('/users', userController);

module.exports = router;
