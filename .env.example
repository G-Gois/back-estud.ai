# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
DB_POOL_SIZE=10

# Optionally replace the individual settings above with a full URI
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Misc
NODE_ENV=development
API_KEY=change-me
