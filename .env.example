# PostgreSQL connection settings (local development)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=estud_ai
DB_POOL_SIZE=10

# DATABASE_URL (usado em produção - Railway/Render provisionam automaticamente)
# Deixe comentado em desenvolvimento, será preenchido automaticamente no deploy
# DATABASE_URL=postgres://user:password@host:5432/dbname

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Server Configuration
PORT=3000
# HOST será automaticamente 0.0.0.0 em produção (NODE_ENV=production)
HOST=127.0.0.1
NODE_ENV=development
